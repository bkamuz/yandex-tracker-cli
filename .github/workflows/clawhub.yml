name: Publish to ClawHub
on:
  push:
    branches: [ main, master ]

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Install clawhub
        run: npm install -g clawhub
      - name: Bump version (each segment 0-9, carry on overflow)
        id: bump
        run: |
          CUR=$(jq -r '.version' _meta.json)
          IFS=. read -ra P <<< "$CUR"
          i=$((${#P[@]} - 1))
          P[i]=$((${P[i]} + 1))
          while (( i > 0 && P[i] > 9 )); do P[i]=0; i=$((i-1)); P[i]=$((${P[i]} + 1)); done
          (( P[0] > 9 )) && P[0]=0
          NEW=$(IFS=.; echo "${P[*]}")
          echo "version=$NEW" >> $GITHUB_OUTPUT
          echo "Bumped $CUR -> $NEW"
          jq --arg v "$NEW" '.version = $v' _meta.json > _meta.json.tmp && mv _meta.json.tmp _meta.json
      - name: Publish to ClawHub
        env:
          CLAWHUB_TOKEN: ${{ secrets.CLAWHUB_TOKEN }}
        run: |
          clawhub login --token "$CLAWHUB_TOKEN"
          clawhub publish . --slug yandex-tracker-cli --name "Yandex Tracker CLI" --version "${{ steps.bump.outputs.version }}" --tags "bash,curl,yandex,tracker,cli"
      - name: Commit version bump
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add _meta.json
          git diff --staged --quiet || ( git commit -m "chore: bump version to ${{ steps.bump.outputs.version }}" && git push )
